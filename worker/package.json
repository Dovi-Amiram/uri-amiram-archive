{
  "name": "uri-amiram-archive-api",
  "private": true,
  "version": "1.0.0",
  "description": "Tiny authenticated write API: commits new archive entries to GitHub",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260914.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0",
    "wrangler": "^4.131.2"
  }
}
